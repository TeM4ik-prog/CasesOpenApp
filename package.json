{
  "name": "casesopenapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git add . && git commit -m \"new commit\" && git push -u origin main"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "git": "^0.1.5"
  }
}
